rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read and write all files
    // Admin access is controlled at frontend level via secret code
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
